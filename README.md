# crateOS
CrateOS is a curated server platform built on Ubuntu Server LTS: a Pitboy/DOS-style console + optional web panel, one canonical root (/srv/crateos) for config/state/logs, modular service “crates,” MAC-pinned headless networking, and managed nginx + docker compose.
